# Built binaries — leading slash anchors to repo root so this only matches
# the locally-built `ps3` binary, NOT the cmd/ps3/ source directory.
/ps3
/ps3.exe
/bin/
/dist/

# Local config / credentials
.env
.env.*
*.local
config.yaml

# IDE / OS
.vscode/
.idea/
*.swp
.DS_Store
